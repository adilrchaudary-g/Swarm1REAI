# Pipeline

Stage-oriented folders for local lead handling outside Discord.

These folders mirror the practical downstream states that matter for a wholesale operator:

- `intake`
- `enriched`
- `queue`
- `done`
